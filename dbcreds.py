# dbcreds.py

# Database connection credentials
user = 'root'
password = 'password'
host = 'localhost'  # Use "localhost" if the database is on the same machine
port = 3306  # Change this to the port where your MariaDB server is running
database = 'foodie'
